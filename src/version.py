"""Canonical KAE core version.

Single source of truth for the version plugins declare compatibility against
(RFC 0010 §6.1) and that build lockfiles record (RFC 0012).
"""

KAE_CORE_VERSION = "0.1.0"
