# Day 6 — Sat 30 Aug 2026: Contract Testing + Determinism + Vision + Kaggle

## Baseline
- 329 passed, 1 skipped

## RFC 0014 Idempotency (tests/contract/test_analyzer_idempotency.py)
- 21 parametrized tests: double-run each analyzer → identical AST hash
- Fixes applied:
  - **LLMRefinementAnalyzer**: sets `llm_refined_at` timestamp, skips already-refined blocks
  - **TitlePageAnalyzer**: skips pages that already have a TitlePageBlock
  - **BlockClassifierAnalyzer**: skips containers that already have a TOC child

## RFC 0009 Bit Determinism (tests/contract/test_bit_determinism.py)
- 4 tests: same input doc → identical LaTeX output, hash-stable across runs

## Ollama Vision Router (src/agents/manager/router.py)
- `AgentRouter`: discovers vision models across hosts, routes by role (text/vision/table)
- `vision_generate()`: POST /api/generate with base64 image
- `formula_vision_fallback()`: extract LaTeX from formula image via vision model
- Vision model set: llava, llava:7b, llava:13b, llava-llama3, bakllava

## Kaggle GPU-Fallback (src/agents/manager/backends/kaggle.py)
- Added bin/push-kaggle-runner.sh subprocess path (opt-in via KAE_KAGGLE_USE_SCRIPT)
- Not activated by default — requires explicit KAE_MANAGER_BACKEND=kaggle

## COMPLIANCE_AUDIT.md
- Created with RFC 0001/0002/0005/0006/0007/0009/0010/0012/0014/0020/0021/0022 all ✅

## Tests: +35 (21 idempotency + 4 determinism + 10 vision router)

## Multimodel Vision Support (session 2)
- `AgentRouter.discover_vision()`: checks agents.json for multimodel runners before ollama fallback
- `vision_generate()`: supports `/infer` endpoint for multimodel runners, `/api/generate` for ollama
- `route()` returns `kind` field ("ollama" or "multimodel")
- `VisionFallbackAnalyzer`: passes `kind` through to vision_generate
- Kaggle Multi agent (Qwen2.5-VL-7B) registered in agents.json, tested via Cloudflare tunnel

## PageAgent Resilience
- Per-page try/except with circuit breaker: 3 consecutive failures → abort remaining pages
- Prevents pipeline hang when vision agent is unreachable

## TableDetector False-Positive Fix
- Single-column blocks with <5 rows and no separators: rejected
- Single-column blocks with avg text <15 chars and no separators: rejected
- Before: diagram labels ("Register", "Datum", "Memory") detected as tables
- After: 4 real tables in 568-page book (was dozens of false positives)
- Test updated: `test_3_row_table_with_separator_detected` + `test_3_short_single_col_blocks_not_table`

## Deploy & Smoke Test
- Deployed to rpi5, processed microcomputersys book (568 pages)
- Pipeline completed: 280 paragraphs, 7 diagrams, 4 tables, 33 TOC entries
- Vision skipped (Kaggle runner idle timeout) — circuit breaker works correctly

## Vision Retry & JPEG Optimization (session 3)
- `call_infer()`: 3 attempts × 20s timeout (was single attempt), skip /ocr on timeout
- `_pixmap_to_jpeg()`: quality=30, max_dim=512, DPI 72 — payload ~15KB (was ~200KB PNG)
- Bearer token auth for Cloudflare-tunneled runner
- PDP-11 test book (34 pages): page 5 saved by retry (timeout → table), page 4 systematically fails
- Removed llava:7b from orangepi — ARM CPU vision inference too slow (~minutes)

## COMPLIANCE_AUDIT.md Update
- RFC 0009 §5.2 (bit determinism): 🔧 → ✅
- RFC 0014 §Idempotency: ❌ → ✅

## Final
- Total: 417 passed, 1 skipped
